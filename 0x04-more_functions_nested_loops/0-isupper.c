#include "main.h"
/**
  *_isupper - program to determine is an alphabet is upper starts
  *
  *@c: integer parameter in our function
  *
  *Return: returns 1 if c is upper and 0 otherwise
  */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
		return (0);
}
